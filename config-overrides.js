module.exports = (config) => {
  // 单独配置装饰器
  // 如果没有使用customize-cra,就  在这里可以对config进行配置
  return config
}

//自定义的react-config
// const { override, addDecoratorsLegacy } = require ('customize-cra')//自定义cra,

// module.exports = override(
//   addDecoratorsLegacy()
// )